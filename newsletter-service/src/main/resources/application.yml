spring:
  jpa:
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost:5432}/${DB_NAME:shapeit-api}
    username: ${DB_USERNAME:postgres}
    password: ${DB_PASSWORD:postgres}
    driver-class-name: org.postgresql.Driver
  security:
    oauth2:
      client:
        registration:
          mail-client:
            provider: coachcore
            client-id: ${OAUTH2_MAIL_CLIENT_ID:coachcore-mail-sender}
            client-secret: ${OAUTH2_MAIL_CLIENT_SECRET:secret}
            authorization-grant-type: client_credentials
            scope: mail.send
        provider:
          coachcore:
            token-uri: ${AUTHORIZATION_SERVER_HOST:http://localhost:9000}${TOKEN_URI:/oauth2/token}
      resourceserver:
        jwt:
          issuer-uri: ${AUTHORIZATION_SERVER_HOST:http://localhost:9000}
          jwk-set-uri: ${AUTHORIZATION_SERVER_HOST:http://localhost:9000}${JWKS_SET_URI:/oauth2/jwks}
logging:
  level:
    org:
      springframework:
        security: DEBUG
oauth2-mail-client:
  client-id: ${OAUTH2_MAIL_CLIENT_ID}
  client-secret: ${OAUTH2_MAIL_CLIENT_SECRET}
server:
  port: 8000